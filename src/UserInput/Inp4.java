void main() throws IOException
{
    BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));
    
//    String str = reader.readLine();
    
    IO.println("Enter any paragraph");
    String str = reader.readLine();
    IO.println(str);
}